import React from 'react'
import { Link } from 'gatsby'
import PropTypes from 'prop-types'

import Nav from './Nav'

const Header = () => (
  <div>
    Header
    <Nav />
  </div>
)

export default Header
