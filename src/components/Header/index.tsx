import * as React from 'react';
import { Flex, Heading } from 'theme-ui';

export default function Header(): React.ReactElement {
  return (
    <Flex>
      <Heading>Adam Anderson</Heading>
    </Flex>
  );
}
